{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://playbook.dev/schemas/memory-pressure-status.schema.json",
  "title": "PlaybookMemoryPressureStatus",
  "description": "Deterministic read-only memory pressure policy status with hysteresis-based banding and canonical-artifact preservation invariants.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "kind", "command", "generatedAt", "reviewOnly", "authority", "policy", "usage", "score", "band", "previousBand", "classes", "invariants", "recommendedActions"],
  "properties": {
    "schemaVersion": { "const": "1.0" },
    "kind": { "const": "playbook-memory-pressure-status" },
    "command": { "const": "memory-pressure-evaluate" },
    "generatedAt": { "type": "string", "format": "date-time" },
    "reviewOnly": { "const": true },
    "authority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["mutation", "promotion"],
      "properties": {
        "mutation": { "const": "read-only" },
        "promotion": { "const": "review-required" }
      }
    },
    "policy": {
      "type": "object",
      "additionalProperties": false,
      "required": ["budgetBytes", "budgetFiles", "budgetEvents", "watermarks", "hysteresis"],
      "properties": {
        "budgetBytes": { "type": "integer", "minimum": 1 },
        "budgetFiles": { "type": "integer", "minimum": 1 },
        "budgetEvents": { "type": "integer", "minimum": 1 },
        "watermarks": {
          "type": "object",
          "additionalProperties": false,
          "required": ["warm", "pressure", "critical"],
          "properties": {
            "warm": { "type": "number", "minimum": 0 },
            "pressure": { "type": "number", "minimum": 0 },
            "critical": { "type": "number", "minimum": 0 }
          }
        },
        "hysteresis": { "type": "number", "minimum": 0 }
      }
    },
    "usage": {
      "type": "object",
      "additionalProperties": false,
      "required": ["usedBytes", "fileCount", "eventCount"],
      "properties": {
        "usedBytes": { "type": "integer", "minimum": 0 },
        "fileCount": { "type": "integer", "minimum": 0 },
        "eventCount": { "type": "integer", "minimum": 0 }
      }
    },
    "score": {
      "type": "object",
      "additionalProperties": false,
      "required": ["bytes", "files", "events", "normalized"],
      "properties": {
        "bytes": { "type": "number", "minimum": 0 },
        "files": { "type": "number", "minimum": 0 },
        "events": { "type": "number", "minimum": 0 },
        "normalized": { "type": "number", "minimum": 0 }
      }
    },
    "band": { "enum": ["normal", "warm", "pressure", "critical"] },
    "previousBand": { "enum": ["normal", "warm", "pressure", "critical", null] },
    "classes": {
      "type": "object",
      "additionalProperties": false,
      "required": ["canonical", "compactable", "disposable"],
      "properties": {
        "canonical": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "compactable": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "disposable": { "type": "array", "items": { "type": "string", "minLength": 1 } }
      }
    },
    "invariants": {
      "type": "object",
      "additionalProperties": false,
      "required": ["canonicalEvictionCandidates", "disposableEvictionCandidates", "disposableEvictionRequiresSummary"],
      "properties": {
        "canonicalEvictionCandidates": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "disposableEvictionCandidates": { "type": "array", "items": { "type": "string", "minLength": 1 } },
        "disposableEvictionRequiresSummary": { "const": true }
      }
    },
    "recommendedActions": {
      "type": "array",
      "items": {
        "enum": [
          "dedupe-low-signal-memory",
          "increase-compaction-cadence",
          "summarize-runtime-events-into-rollups",
          "tighten-low-value-admission",
          "preserve-canonical-artifacts",
          "aggressive-compaction",
          "evict-disposable-after-summary"
        ]
      }
    }
  }
}
