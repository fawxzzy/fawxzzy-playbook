{
  "name": "playbook-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm run sync:templates && pnpm -r build",
    "lint": "pnpm -r exec eslint .",
    "smoke": "pnpm -r build && node scripts/smoke-test.mjs",
    "smoke:ci": "pnpm -r build && node packages/cli/dist/main.js --help && node scripts/smoke-test.mjs",
    "test": "pnpm -r test",
    "sync:templates": "node scripts/sync-templates.mjs"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.30.1",
    "@typescript-eslint/parser": "^8.30.1",
    "@types/node": "^22.13.10",
    "eslint": "^9.24.0",
    "typescript": "^5.8.2",
    "vitest": "^3.0.8"
  }
}
